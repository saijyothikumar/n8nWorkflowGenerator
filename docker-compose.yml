version: "3.9"

services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env

  web:
    build: ./frontend
    depends_on:
      - api
    ports:
      - "4200:4200"
    env_file:
      - .env